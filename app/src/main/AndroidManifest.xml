<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 自动更新：允许请求安装 APK 包 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- v22 新增：排课提醒通知权限（Android 13+ / API 33+ 需运行时申请） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- v25 新增：局域网训练计划截图同步 -->
    <!-- FOREGROUND_SERVICE：Android 9+ 启动前台 Service（UdpPlanListenerService）必需 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 前台 Service 类型声明（数据同步类） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 允许应用在锁屏状态下唤醒 CPU 接收 UDP 广播（可选，提升到达率） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- WiFi 多播锁：接收 255.255.255.255 广播包必需（部分手机省电模式默认禁用） -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".SportsCoachApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsCoach"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SportsCoach">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- === v28 优化4：通知点击深链跳转 === -->
            <!-- 用于 ScheduleReminderWorker 发送的通知 PendingIntent 点击后跳转到今日排课页面 -->
            <intent-filter>
                <action android:name="com.shangmentiyu.sportscoach.ACTION_VIEW_TODAY_SCHEDULE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- v25 新增：局域网训练计划截图 UDP 监听 Service -->
        <!-- 前台 Service，长期监听电脑端 lan_plan_sender.py 发送的 UDP 广播 -->
        <!-- 收到广播后弹出系统通知，提示用户在设置页点击"同步电脑端截图"按钮 -->
        <service
            android:name=".core.UdpPlanListenerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- v32 优化3 新增：UDP 局域网设备自动发现 Service -->
        <!-- 前台 Service，长期监听桌面端 desktop_beacon.py 发送的"在线心跳"广播 -->
        <!-- 收到广播后写入 SharedPreferences，UI 顶部状态栏显示绿色"已连接"指示灯 -->
        <service
            android:name=".core.UdpDesktopDiscoveryService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
